// Loads Particles from particles.json configration

particlesJS.load('particles-js',
                 'particles.json',
                  () => {
                         console.log("Particles.json init complete.");
                  });